# Codex Companion v0.1.8

- 修复前端升级后的缓存策略：入口 HTML、Service Worker、注册脚本、版本文件不再长期缓存，只有带内容哈希的资源使用 immutable 缓存。
- PWA 导航优先获取网络页面，离线时使用缓存，避免一直显示旧入口。
- 侧栏显示网页版本和“刷新页面”入口，发现新版本时提示刷新，不自动打断输入或清除浏览器配对。
- 为长会话列表补充收缩限制，额度卡片及底部操作保持可见。
- 回归测试覆盖 175 个会话、截图式长 JSON 消息、长标题、320–1280 像素视口和版本更新，并检查真实 Nginx 缓存响应头。

本次修复只需更新 Web。已有 v0.1.7 Relay、Bridge 可继续使用；使用 `--no-deps` 更新 Web 可保留模式 A 的内存配对。更新后用新链接打开网页，确认侧栏出现“网页 v0.1.8”及“剩余用量”。
